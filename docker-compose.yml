services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: kimera-db
    restart: always
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: kimera
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: